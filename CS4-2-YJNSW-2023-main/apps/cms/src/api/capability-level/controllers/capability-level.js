"use strict";

/**
 *  capability-level controller
 */

const { createCoreController } = require("@strapi/strapi").factories;

module.exports = createCoreController("api::capability-level.capability-level");
