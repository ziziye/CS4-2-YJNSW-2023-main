"use strict";

/**
 *  capability-type controller
 */

const { createCoreController } = require("@strapi/strapi").factories;

module.exports = createCoreController("api::capability-type.capability-type");
